# Core LLM + orchestration
groq>=0.9.0
langgraph>=0.2.0
langchain>=0.3.0
langchain-community>=0.3.0
langchain-groq>=0.2.0

# MCP — fastmcp supplies the in-memory transport used by mcp_bridge.py
fastmcp>=2.0.0
mcp>=1.2.0

# Guardrails — NVIDIA NeMo Guardrails runs the input/output rails defined in
# guardrails/. The rails execute local Python actions rather than NeMo's stock
# self_check_* prompts, so no LLM engine is configured for them.
nemoguardrails>=0.11.0

# Observability
# v4 floor is required, not cosmetic: observability.py and llm.py call
# start_observation(as_type=...) and propagate_attributes(), neither of which
# exists in v3. On v3 every call would fail into its except branch and tracing
# would silently no-op again — exactly the bug this project already hit once.
langfuse>=4.0.0,<5.0.0

# Market data
yfinance>=0.2.40
mftool>=2.8.0
feedparser>=6.0.0
requests>=2.31.0
beautifulsoup4>=4.12.0
pandas>=2.1.0
numpy>=1.24.0
ta>=0.11.0

# Required for NSE: Akamai fingerprints the TLS handshake and rejects plain
# `requests` with 403. curl_cffi presents a real Chrome fingerprint.
curl_cffi>=0.7.0

# RAG — embeddings come from the Hugging Face Inference API (no local model),
# vectors are stored in Pinecone (no local index).
pinecone>=5.0.0

# Web layer
fastapi>=0.115.0
uvicorn>=0.30.0
pydantic>=2.5.0
pydantic-settings>=2.0.0
sqlalchemy>=2.0.0
python-dotenv>=1.0.0
httpx>=0.27.0
PyYAML>=6.0.0

# Removed in v4:
#   faster-whisper  -> voice mode removed
#   kokoro          -> voice mode removed
#   soundfile       -> voice mode removed
